---
phase: 03-core-scanning-engine
plan: 07
type: execute
wave: 5
depends_on: ["03-06"]
files_modified:
  - frontend/src/App.tsx
  - frontend/src/components/AppLayout.tsx
autonomous: false

must_haves:
  truths:
    - "User can navigate to /scan from the app navigation bar"
    - "ScanPage renders correctly at /scan route with AppLayout"
    - "Nav link 'Scan' is highlighted when on /scan route"
    - "User can trigger a time-range scan from ScanPage"
    - "User can trigger a URL analysis from ScanPage"
    - "Loading state is visible while scan is in progress"
    - "Results display in card grid when scan completes"
    - "Error message appears when scan fails"
  artifacts:
    - path: "frontend/src/App.tsx"
      provides: "/scan route wired with ProtectedRoute and AppLayout wrapping ScanPage"
      contains: "ScanPage"
    - path: "frontend/src/components/AppLayout.tsx"
      provides: "Scan NavLink added to center navigation"
      contains: "/scan"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "frontend/src/pages/ScanPage.tsx"
      via: "React Router Route at /scan"
      pattern: "path.*scan.*ScanPage"
    - from: "frontend/src/components/AppLayout.tsx"
      to: "/scan"
      via: "NavLink to='/scan'"
      pattern: "NavLink.*to.*scan"
---

<objective>
Wire ScanPage into the React Router routes and add a "Scan" navigation link in AppLayout. Then verify the complete end-to-end scan flow works.

Purpose: Without routing and nav, ScanPage is unreachable. This plan connects all Phase 3 work into a usable feature. The checkpoint verifies the full flow: navigate -> scan -> progress -> results.

Output: App.tsx updated with /scan route, AppLayout.tsx updated with Scan nav link, human verification of full scan flow.
</objective>

<execution_context>
@C:/Users/shadi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/shadi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-core-scanning-engine/03-06-SUMMARY.md
@frontend/src/App.tsx
@frontend/src/components/AppLayout.tsx
@frontend/src/pages/ScanPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add /scan route and Scan nav link</name>
  <files>
    frontend/src/App.tsx
    frontend/src/components/AppLayout.tsx
  </files>
  <action>
    1. Update frontend/src/App.tsx:
       - Add import: `import ScanPage from './pages/ScanPage';`
       - Add route between /dashboard and /settings/integrations:
         ```tsx
         <Route
           path="/scan"
           element={
             <ProtectedRoute>
               <AppLayout>
                 <ScanPage />
               </AppLayout>
             </ProtectedRoute>
           }
         />
         ```

    2. Update frontend/src/components/AppLayout.tsx:
       - Add a "Scan" NavLink in the center navigation section, between the "Dashboard" and "Settings" links:
         ```tsx
         <NavLink
           to="/scan"
           className={({ isActive }) =>
             `text-sm font-medium transition-colors ${
               isActive ? 'text-blue-600 border-b-2 border-blue-600 pb-0.5' : 'text-gray-600 hover:text-gray-900'
             }`
           }
         >
           Scan
         </NavLink>
         ```
       Use the exact same NavLink pattern/className as the existing Dashboard and Settings links.
  </action>
  <verify>
    ```bash
    cd frontend && npx tsc --noEmit
    ```
    No TypeScript errors.

    ```bash
    grep -c "ScanPage" frontend/src/App.tsx
    grep -c "/scan" frontend/src/components/AppLayout.tsx
    ```
    Both should return 1 or more.
  </verify>
  <done>/scan route exists in App.tsx wrapped in ProtectedRoute + AppLayout; AppLayout nav shows Dashboard | Scan | Settings links</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of complete Phase 3 scan flow</name>
  <action>
    Complete Phase 3 - Core Scanning Engine:
    - Celery + Redis infrastructure for background scan jobs
    - Apify (primary) + PhantomBuster (fallback) integrations
    - Viral scoring algorithm (engagement rate * velocity multiplier, 0-100)
    - S3 thumbnail caching
    - FastAPI endpoints: POST /scans/trigger, POST /scans/analyze-url, GET /scans/status/{id}, GET /scans/history
    - Frontend: ScanPage, ScanForm (time range + URL tabs), ScanProgress (skeleton), ViralPostCard, ViralPostGrid
    - useScan hook with 2-second polling
    - Nav link "Scan" in AppLayout
  </action>
  <verify>
    Prerequisites:
    1. Ensure Redis is running: open a terminal and run `redis-cli ping` — should return PONG
    2. Start Celery worker in a separate terminal:
       `cd backend && .venv/Scripts/celery -A app.celery_app worker --loglevel=info`
    3. Start FastAPI: `cd backend && .venv/Scripts/uvicorn app.main:app --reload`
    4. Start frontend: `cd frontend && npm run dev`
    5. Ensure .env has APIFY_API_KEY set (get free key at https://console.apify.com)

    Test steps:
    1. Open http://localhost:5173, log in with your account
    2. Verify "Scan" link appears in the nav bar (between Dashboard and Settings)
    3. Click "Scan" — verify ScanPage loads at /scan
    4. In the "Discover Viral Posts" tab, select "24h" time range
    5. Click "Start Discovery Scan"
    6. Verify: progress bar appears and skeleton cards are visible (SCAN-07 progress indicator)
    7. Wait for scan to complete (5-30 seconds depending on Apify)
    8. Verify: green "Scan complete" banner appears with post count
    9. Verify: viral post cards appear in a grid with thumbnails (UX-10), creator info, engagement metrics, viral score (UX-01)
    10. Click "View on Instagram" on any card — verify it opens the post in a new tab
    11. Click "New Scan" — verify form resets and shows the scan form again

    URL analysis test:
    12. Click "Analyze Specific Post" tab
    13. Paste a valid Instagram post URL (e.g. from step 10 above)
    14. Click "Analyze Post"
    15. Verify progress shows, then result card displays

    Error handling test (SCAN-08):
    16. Try entering an invalid URL (e.g. "https://google.com") — verify client-side error message appears
    17. Check browser console for no uncaught errors

    Navigation test:
    18. Navigate away to Dashboard and back to Scan — verify scan results are preserved in Zustand store

    Type "approved" if all tests pass. Or describe specific issues found (e.g. "thumbnails not showing", "scan stuck at pending", "nav link missing")
  </verify>
  <done>Human has confirmed: nav shows Scan link, scan flow completes end-to-end with results grid and thumbnails, URL analysis works, error states display correctly</done>
</task>

</tasks>

<verification>
Phase 3 complete when:
1. `cd frontend && npx tsc --noEmit` zero errors
2. `/scan` route accessible and renders ScanPage inside AppLayout
3. Nav shows Dashboard | Scan | Settings — all three links active-highlighted correctly
4. Full scan flow confirmed by human: trigger -> progress -> results grid with thumbnails
5. URL analysis flow confirmed: URL input -> progress -> single result card
6. Error handling confirmed: invalid URL shows client error, failed scan shows retry button
</verification>

<success_criteria>
- /scan route in App.tsx wrapped in ProtectedRoute + AppLayout
- AppLayout nav has 3 links: Dashboard, Scan, Settings (in order)
- User can complete full scan discovery flow end-to-end
- User can complete URL analysis flow end-to-end
- Scan results persist in Zustand store across page navigation
- Human verifier has approved the flow
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-scanning-engine/03-07-SUMMARY.md`
</output>
