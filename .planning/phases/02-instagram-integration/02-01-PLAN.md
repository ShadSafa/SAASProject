---
phase: 02-instagram-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/instagram_account.py
  - backend/migrations/versions/002_instagram_account_enhancements.py
autonomous: true

must_haves:
  truths:
    - "InstagramAccount model has status field (active/expired/revoked enum)"
    - "InstagramAccount model stores profile_picture, account_type, follower_count"
    - "InstagramAccount model stores encrypted access_token (LargeBinary) with token_expires_at"
    - "A new Alembic migration adds the new columns to instagram_accounts table"
  artifacts:
    - path: "backend/app/models/instagram_account.py"
      provides: "Enhanced InstagramAccount SQLAlchemy model"
      contains: "AccountStatus enum, status column, profile_picture, account_type, follower_count, LargeBinary access_token"
    - path: "backend/migrations/versions/002_instagram_account_enhancements.py"
      provides: "Database migration adding new columns"
      contains: "op.add_column instagram_accounts"
  key_links:
    - from: "backend/app/models/instagram_account.py"
      to: "backend/app/database.py"
      via: "Base import"
      pattern: "from app\\.database import Base"
    - from: "backend/migrations/versions/002_instagram_account_enhancements.py"
      to: "backend/app/models/instagram_account.py"
      via: "Alembic migration mirrors model columns"
      pattern: "op\\.add_column.*instagram_accounts"
---

<objective>
Enhance the existing InstagramAccount database model with the additional fields required for Phase 2 OAuth integration: account status (active/expired/revoked), Instagram profile data (picture, account type, follower count), and secure token storage using binary columns.

Purpose: The existing model from Phase 01 has minimal fields. Phase 2 needs status tracking, profile display data, and proper token lifecycle fields (expires_at with binary storage for future encryption).

Output: Updated SQLAlchemy model + Alembic migration file (not applied yet — executor applies it once database is available).
</objective>

<execution_context>
@C:/Users/shadi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/shadi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-database/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance InstagramAccount model with status, profile data, and token fields</name>
  <files>backend/app/models/instagram_account.py</files>
  <action>
Replace the existing InstagramAccount model entirely with an enhanced version. The existing model has: id, user_id, instagram_user_id, instagram_username, access_token (String), token_expires_at, created_at, updated_at.

Add the following:

1. Add AccountStatus enum (before the class definition):
   ```python
   import enum
   class AccountStatus(str, enum.Enum):
       active = "active"
       expired = "expired"
       revoked = "revoked"
   ```

2. Update columns:
   - Keep: id, user_id (FK with CASCADE), instagram_user_id, created_at, updated_at
   - Rename: instagram_username -> username (cleaner field name — NOTE: migration must handle this)
   - Change: access_token from String to LargeBinary (for future encryption; store raw bytes)
   - Add: profile_picture = Column(String, nullable=True) — URL to profile image
   - Add: account_type = Column(String, nullable=True) — "Personal", "Creator", or "Business"
   - Add: follower_count = Column(Integer, nullable=True)
   - Keep: token_expires_at = Column(DateTime, nullable=True)
   - Add: status = Column(Enum(AccountStatus), default=AccountStatus.active, nullable=False)

3. Import additions needed:
   - Add `import enum` at top
   - Add `Enum, LargeBinary` to sqlalchemy imports
   - Keep existing imports (Integer, String, DateTime, ForeignKey, Index, UniqueConstraint)

4. Keep existing relationships (user -> back_populates="instagram_accounts") and __table_args__ (index on user_id, unique constraint on user_id+instagram_user_id).

5. Update __repr__ to use username instead of instagram_username.

The model should NOT have a refresh_token column — Instagram long-lived tokens are refreshed using the same access token via /refresh_access_token endpoint; no separate refresh token is needed.
  </action>
  <verify>
    Run: cd backend && python -c "from app.models.instagram_account import InstagramAccount, AccountStatus; print('Model OK'); print([s.value for s in AccountStatus])"
    Expected output: Model OK followed by ['active', 'expired', 'revoked']
  </verify>
  <done>InstagramAccount model imports without error and AccountStatus enum has three values: active, expired, revoked.</done>
</task>

<task type="auto">
  <name>Task 2: Create Alembic migration for enhanced instagram_accounts schema</name>
  <files>backend/migrations/versions/002_instagram_account_enhancements.py</files>
  <action>
Create a new migration file manually (no database connection available). The migration should handle transitioning the existing instagram_accounts table to match the enhanced model.

Migration file path: backend/migrations/versions/002_instagram_account_enhancements.py

Migration logic:

**upgrade():**
1. Add new columns to instagram_accounts:
   - `profile_picture` VARCHAR nullable
   - `account_type` VARCHAR nullable
   - `follower_count` INTEGER nullable
   - `status` VARCHAR NOT NULL DEFAULT 'active' (use server_default='active' for existing rows)
2. Rename column: instagram_username -> username
3. Change access_token column type from VARCHAR to BYTEA (LargeBinary in PostgreSQL)

Use op.add_column(), op.alter_column(), op.execute() as appropriate.

For the rename: use op.alter_column(table_name, 'instagram_username', new_column_name='username', existing_type=sa.String())

For the type change: op.alter_column('instagram_accounts', 'access_token', type_=sa.LargeBinary(), postgresql_using='access_token::bytea')

**downgrade():**
Reverse each operation in reverse order:
1. Rename username back to instagram_username
2. Change access_token back to VARCHAR
3. Drop added columns (profile_picture, account_type, follower_count, status)

Migration metadata:
```python
revision = '002_instagram_enhancements'
down_revision = 'df1349f0b6a4'  # The revision ID from 001_initial_schema (check alembic history)
branch_labels = None
depends_on = None
```

NOTE: The executor should first run `alembic history` to confirm the down_revision ID of the first migration before writing this file, and use the actual ID from the output.
  </action>
  <verify>
    Run: cd backend && alembic history
    Expected: Two migrations listed — the initial schema migration and the new 002 migration, with 002 as head.
  </verify>
  <done>alembic history shows 002_instagram_account_enhancements as the head migration depending on the initial schema migration.</done>
</task>

</tasks>

<verification>
1. cd backend && python -c "from app.models.instagram_account import InstagramAccount, AccountStatus; print(InstagramAccount.__table__.columns.keys())"
   - Expected output includes: ['id', 'user_id', 'instagram_user_id', 'username', 'access_token', 'profile_picture', 'account_type', 'follower_count', 'token_expires_at', 'status', 'created_at', 'updated_at']

2. cd backend && alembic history
   - Expected: Two entries, 002 as head

3. cd backend && python -c "from app.models import User, InstagramAccount; print('All models load OK')"
   - Expected: All models load OK (ensures no import errors)
</verification>

<success_criteria>
1. InstagramAccount model has AccountStatus enum with active/expired/revoked values
2. Model has profile_picture, account_type, follower_count, status columns
3. access_token column is LargeBinary
4. username field replaces instagram_username
5. Migration file 002 exists and appears in alembic history as head
6. All existing model imports still work (User, Scan, etc.)
</success_criteria>

<output>
After completion, create `.planning/phases/02-instagram-integration/02-01-SUMMARY.md` using the summary template.
</output>
